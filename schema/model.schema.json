{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Neural Model Schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["model_name", "modality", "architecture", "year", "paper_url"],
  "properties": {
    "model_name": {
      "type": "string",
      "minLength": 1,
      "description": "Full name of the model."
    },
    "modality": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Modalities ingested in the model."
    },
    "architecture": {
      "type": "string",
      "minLength": 1,
      "description": "Core neural architecture (CNN, Transformer, etc.)."
    },
    "task": {
      "type": "string",
      "minLength": 1,
      "description": "Main task (classification, reconstruction, decoding, etc.)."
    },
    "year": {
      "type": "integer",
      "minimum": 1980,
      "maximum": 2100
    },
    "paper_doi": {
      "type": "string",
      "minLength": 1,
      "description": "DOI of the paper (optional)."
    },
    "paper_url": {
      "type": "string",
      "format": "uri"
    },
    "code_url": {
      "type": "string",
      "format": "uri"
    },
    "dataset_tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Dataset identifiers referenced by this model entry."
    },
    "open_weights": {
      "type": "boolean"
    },
    "notes": {
      "type": "string",
      "minLength": 1
    }
  }
}
